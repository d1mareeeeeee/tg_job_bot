# Основные зависимости
httpx==0.27.2             # HTTP-клиент: чтение t.me/s/ и отправка через Bot API
beautifulsoup4==4.12.3    # парсинг HTML веб-превью каналов (t.me/s/<username>)
python-dotenv==1.0.1      # чтение конфигурации из .env

# LLM SDK — опциональны, нужен только выбранный провайдер (LLM_PROVIDER).
# Можно закомментировать неиспользуемые.
openai==1.54.4            # LLM_PROVIDER=openai
anthropic==0.39.0         # LLM_PROVIDER=anthropic
google-genai==0.3.0       # LLM_PROVIDER=gemini
